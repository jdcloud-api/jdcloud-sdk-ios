/* Copyright 2018 JDCLOUD.COM

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

     http:#www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.

   大屏数据查询接口
   陕西苹果大屏数据查询接口

   OpenAPI spec version: v1
   Contact: 

   NOTE: This class is auto generated by the jdcloud code generator program.
 */


import Foundation
import JDCloudSDKCore

/// 根据区域、行业、一级指标、二级指标、起始时间等条件查询数据 
public class GetLargeScreenDataResult:NSObject,JdCloudResult
{
    /// true为成功，false为失败
    var status:Bool?

    /// 描述信息
    var message:String?

    /// 查询数据结果
    var data:RegionIndustryDataList?



    public override init(){
        super.init()
    }

    enum GetLargeScreenDataResultCodingKeys: String, CodingKey {
        case status
        case message
        case data
    }

    required public init(from decoder: Decoder) throws {
        let decoderContainer = try decoder.container(keyedBy: GetLargeScreenDataResultCodingKeys.self)
        if decoderContainer.contains(.status)
        {
            self.status = try decoderContainer.decode(Bool?.self, forKey: .status)
        }
        if decoderContainer.contains(.message)
        {
            self.message = try decoderContainer.decode(String?.self, forKey: .message)
        }
        if decoderContainer.contains(.data)
        {
            self.data = try decoderContainer.decode(RegionIndustryDataList?.self, forKey: .data)
        }
    }
}
public extension GetLargeScreenDataResult{
    func encode(to encoder: Encoder) throws {
        var encoderContainer = encoder.container(keyedBy: GetLargeScreenDataResultCodingKeys.self)
        try encoderContainer.encode(status, forKey: .status)
        try encoderContainer.encode(message, forKey: .message)
        try encoderContainer.encode(data, forKey: .data)
    }
}
public class GetLargeScreenDataResponse:NSObject,Codable
{
    var requestId:String?;

    var error:ServiceError?;

    var result:GetLargeScreenDataResult?;

    enum GetLargeScreenDataResponseCodingKeys: String, CodingKey {
        case requestId
        case error
        case result
    }

    required public init(from decoder: Decoder) throws {
        let decoderContainer = try decoder.container(keyedBy: GetLargeScreenDataResponseCodingKeys.self)
        self.requestId = try decoderContainer.decodeIfPresent(String?.self, forKey: .requestId) ?? nil
        self.error = try decoderContainer.decodeIfPresent(ServiceError?.self, forKey: .error) ?? nil
        self.result = try decoderContainer.decodeIfPresent(GetLargeScreenDataResult?.self, forKey: .result) ?? nil
    }
}

public extension GetLargeScreenDataResponse{
        func encode(to encoder: Encoder) throws {
        var encoderContainer = encoder.container(keyedBy: GetLargeScreenDataResponseCodingKeys.self)
        try encoderContainer.encode(requestId, forKey: .requestId)
        try encoderContainer.encode(error, forKey: .error)
        try encoderContainer.encode(result, forKey: .result)
    }
}    

///  根据区域、行业、一级指标、二级指标、起始时间等条件查询数据
public class GetLargeScreenDataRequest:JdCloudRequest
{
    /// 查询区域，比如某某省或某某市（可选区域以最终授权为准）
    var region:String

    /// 查询行业，比如某个水果或者农作物（可选行业以最终授权为准）
    var industry:String

    /// 查询起始时间，格式如下：yyyy-MM-dd
    var startDate:String

    /// 查询结束时间，格式如下：yyyy-MM-dd
    var endDate:String

    /// 数据对应的第一级分析指标（可选一级指标以最终授权为准）
    var firstIndex:String

    /// 数据对应的第二级分析指标，如不填写，则默认把一级指标下的所有二级指标都查询出来（可选二级指标以最终授权为准）
    var secondIndex:String?


    public init(regionId: String,region:String,industry:String,startDate:String,endDate:String,firstIndex:String){
        self.region = region
        self.industry = industry
        self.startDate = startDate
        self.endDate = endDate
        self.firstIndex = firstIndex
        super.init(regionId: regionId)
    }


    enum GetLargeScreenDataRequestRequestCodingKeys: String, CodingKey {
        case region
        case industry
        case startDate
        case endDate
        case firstIndex
        case secondIndex
    }

    public override func encode(to encoder: Encoder) throws {
        var encoderContainer = encoder.container(keyedBy: GetLargeScreenDataRequestRequestCodingKeys.self)
        try encoderContainer.encode(region, forKey: .region)
        try encoderContainer.encode(industry, forKey: .industry)
        try encoderContainer.encode(startDate, forKey: .startDate)
        try encoderContainer.encode(endDate, forKey: .endDate)
        try encoderContainer.encode(firstIndex, forKey: .firstIndex)
        try encoderContainer.encode(secondIndex, forKey: .secondIndex)

    }
}

