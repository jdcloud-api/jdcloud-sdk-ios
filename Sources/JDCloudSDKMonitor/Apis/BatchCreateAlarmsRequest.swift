/* Copyright 2018 JDCLOUD.COM

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

     http:#www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.

   Monitoring Rules APIs
   云监控规则相关接口，提供创建、查询、修改、删除监控规则等功能

   
   Contact: 

   NOTE: This class is auto generated by the jdcloud code generator program.
 */


import Foundation
import JDCloudSDKCore


///  批量创建报警规则，可以为多个实例创建多个报警规则。
public class BatchCreateAlarmsRequest:JdCloudRequest
{
    /// 幂等性校验参数，最长36位
    var clientToken:String

    /// 通知的联系人
    var contacts:BaseContact?

    /// 地域
    var datacenter:String?

    /// 是否启用, 1表示启用规则，0表示禁用规则，默认为1
    var enabled:Int64?

    /// 报警规则对应实例列表，每次最多100个，例如&quot;[&#39;resourceId1&#39;,&#39;resourceId2&#39;]&quot;
    var resourceIds:String?

    /// 规则类型, 1表示资源监控，6表示站点监控，默认为1
    var ruleType:Int64?

    /// 要批量创建的规则列表
    var rules:BaseRule?

    /// 是否保存为模板
    var saveTemplate:Bool?

    /// 产品线标识，规则对应的serviceCode
    var serviceCode:String

    /// 模板名称，保存模板时，不能为空
    var templateName:String?

    /// 产品线标识，保存为模板时，模板对应的serviceCode
    var templateServiceCode:String?

    /// 回调content 注：仅webHookUrl和webHookContent均不为空时，才会创建webHook
    var webHookContent:String?

    /// webHook协议
    var webHookProtocol:String?

    /// 回调secret，用户请求签名，防伪造
    var webHookSecret:String?

    /// 回调url
    var webHookUrl:String?


    public init(regionId: String,clientToken:String,rules:BaseRule?,serviceCode:String){
        self.clientToken = clientToken
        self.rules = rules
        self.serviceCode = serviceCode
        super.init(regionId: regionId)
    }


    enum BatchCreateAlarmsRequestRequestCodingKeys: String, CodingKey {
        case clientToken
        case contacts
        case datacenter
        case enabled
        case resourceIds
        case ruleType
        case rules
        case saveTemplate
        case serviceCode
        case templateName
        case templateServiceCode
        case webHookContent
        case webHookProtocol
        case webHookSecret
        case webHookUrl
    }

    public override func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: BatchCreateAlarmsRequestRequestCodingKeys.self)
        try container.encode(clientToken, forKey: .clientToken)
        try container.encode(contacts, forKey: .contacts)
        try container.encode(datacenter, forKey: .datacenter)
        try container.encode(enabled, forKey: .enabled)
        try container.encode(resourceIds, forKey: .resourceIds)
        try container.encode(ruleType, forKey: .ruleType)
        try container.encode(rules, forKey: .rules)
        try container.encode(saveTemplate, forKey: .saveTemplate)
        try container.encode(serviceCode, forKey: .serviceCode)
        try container.encode(templateName, forKey: .templateName)
        try container.encode(templateServiceCode, forKey: .templateServiceCode)
        try container.encode(webHookContent, forKey: .webHookContent)
        try container.encode(webHookProtocol, forKey: .webHookProtocol)
        try container.encode(webHookSecret, forKey: .webHookSecret)
        try container.encode(webHookUrl, forKey: .webHookUrl)

    }
}
